# Known Issues (working file)

Working list for the optional staff Known Issues excel. Column schema matches
`context/Known Issues.docx` (example table). English only.

**Do not submit this markdown.** Before packaging, copy the table into the staff
Google Sheet clone and export `.xlsx` into the zip root. Agent instructions:
`.cursor/skills/populate-known-issues/SKILL.md`.

Rows marked **FIX BEFORE SUBMIT** in the description must be repaired or left
listed; shipping them undocumented is worse than listing them.

| # | Type | Sub Type | Description | Severity | Reproducibility | Reproduce steps | Reason | Reason notes |
|---|------|----------|-------------|----------|-----------------|-----------------|--------|--------------|
| 2 | Bug | Program misbehave | **FIX BEFORE SUBMIT.** Default provided composition `inputs/sim_compose.yaml` (24 cells) completes every cell as Error / `mission_score < 0` (`SPAWN_NOT_PASSABLE` and/or `MISSION_EXCEPTION`). AdvCpp review guideline requires happy-flow completion on default scenarios. | High | Always | `./simulator_207190406_209543255 -comparative simulation=inputs/sim_compose.yaml mission_control_folder=<dir with MissionControl_*.so> algorithm=<Algorithm_*.so>`. Inspect the report: every run unscored. Same in `-competition`. See `docs/integration-verification-report.md`. | Lack of knowledge | House spawn vs drone radius / map offset still produces blocked spawn or MockMovement throws. Needs a completing composition or a spawn/offset fix; unit tests score Completed runs, so the scorer itself is not the failure. |
| 3 | Feature | Partially Missing | Root `README.md` still contains the skeleton placeholder ("You should update this README file") and lowercase-namespace advice. It does not list contributor names/IDs, build presets, or the two CLI invocations. Submission Guidelines require names, IDs, and input/output format in the readme; AdvCpp requires readme build/run instructions to match the program. Output-map naming is already documented. | Medium | Always | Open `README.md`. Compare to assignment zip-root README requirement and `context/Submission Guidelines - Advanced Topics Assignments - 2026B.docx`. | Lack of time | Rewrite README with names + IDs, `cmake --preset default` / build, both CLI lines, `.so`/executable names, and the existing output naming section. |
| 4 | Feature | Missing | No High Level Design PDF in the project root. Submission Guidelines require an HLD PDF in the main folder of the submission (graded e14/e15). | High | Always | `Get-ChildItem` / `ls` at the repo root for `*.pdf`. None present. | Lack of time | Each owner diagrams their components (plugin load, run matrix, MissionControl step loop, algorithm planner); assemble one PDF in the zip root and keep it aligned with the three-project layout. |
| 5 | Feature | Missing | Optional: faulty algorithm movement that would go out of bounds is not ignored. `DroneControlImpl` does not drop the move before calling Movement. | Low | Always | Load a plugin that commands a step past world/map bounds. The command is forwarded to Movement instead of being skipped. | Lack of time | Optional Common-issues row CI2. Ignore the command in `DroneControlImpl` before `executeMovement`. Named `kMaxRetries` not needed for this row. |
| 6 | Feature | Missing | Optional: invalid algorithm command values are not retried N times then thrown. Oversize/unsupported commands return `DroneStepStatus::Error` immediately. | Low | Always | Emit a command with invalid fields. `DroneControlImpl` returns Error on the first bad command; no retry loop and no throw-after-N. | Lack of time | Optional CI3. Re-call `nextStep` up to `kMaxRetries`, then throw so `DroneControl` and `SimulationRun` can catch. |
| 7 | Feature | Missing | Optional: empty movement **and** empty scan (NOOP) is not retried N times. | Low | Always | Algorithm returns a Working command with neither movement nor scan. No NOOP retry; mission proceeds or finishes without the PDF recovery. | Lack of time | Optional CI4. Detect both-empty, retry, throw after N. |
| 8 | Feature | Missing | Optional: empty LiDAR vector is not retried N times. | Low | Always | Arrange a scan that returns `{}`. `DroneControlImpl` does not re-scan or throw after N empty results. | Lack of time | Optional CI6. Retry `lidar.scan` up to `kMaxRetries`, then throw. |
| 9 | Feature | Missing | Optional: Movement returning `false` is not retried N times then thrown. | Low | Always | Use a movement driver that returns `false` (not a wall throw). `DroneControlImpl` maps some "blocked"/"boundary" messages to Continue; otherwise Error. No N-retry then throw. | Lack of time | Optional CI7. Distinct from mandatory CI5 (MockMovement throw). Retry then throw; catch in DroneControl and SimulationRun. |
| 10 | Feature | Missing | Optional: movement larger than drone max is rejected instead of split into two or more legal steps. | Medium | Always | Algorithm emits Advance/Elevate/Rotate above `max_advance` / `max_elevate` / `max_rotate`. `movementWithinLimits` returns Error. | Lack of time | Optional CI8. Split into sequential in-limit commands, each a separate step. |
| 11 | Feature | Partially Missing | Optional: drone step `Error` is logged but the mission stops. PDF says log and continue. | Low | Always | Force a `DroneStepStatus::Error` (e.g. oversize move). `MissionControlImpl::runMission` pushes `DRONE_STEP_FAILED` and returns; it does not keep stepping. | Lack of time | Optional CI9. Log the ErrorRef and continue the `max_steps` loop. |
| 12 | Feature | Missing | Optional: movement that would leave **mission** bounds is not amended. | Low | Always | Command a step that exits `mission_bounds`. No clamp/amend in `DroneControlImpl` (distinct from CI2 ignore-OOB). | Lack of time | Optional CI10. Shorten/clamp the command so the drone stays inside mission bounds. |
| 13 | Feature | Missing | Optional: GPS out-of-bound coordinates are not compared to internal pose. | Low | Always | GPS reports OOB while internal pose is in-bounds (or also OOB). No compare/ignore/throw path. | Lack of time | Optional CI11. Ignore if internal in-bounds; throw if both OOB (targets: DroneControl, SimulationRun). |
| 14 | Feature | Missing | Optional: after a move, impossible GPS is not re-read N times then returned as Error. | Low | Always | Movement succeeds, GPS then reports an impossible pose. No GPS retry loop in `DroneControlImpl`. | Lack of time | Optional CI12. Re-read GPS up to `kMaxRetries`, then return Error (not necessarily throw). Target is DroneControl only. |
| 15 | Feature | Missing | Bonus lazy load/unload of `.so` files is not implemented. Plugins are loaded up front on the main thread and `dlclose`d at shutdown. The assignment explicitly allows eager load; lazy load-once is bonus only. | Low | Always | Read `Simulator/src/main.cpp` and `PluginLoader.cpp`: every required `.so` is opened before `RunMatrixOrchestrator::run`; no unload of unused libraries mid-run. | Lack of time | After the mandatory path is solid, load a `.so` on first use, never reload, `dlclose` when no live objects remain. Document in `bonus.txt` with file:line if claimed. |
| 16 | Feature | developed in a different way | Published `simulator::ISimulation` (`Simulator/common_simulator/include/Simulator/ISimulation.h`) has no implementation. Comparative/competitive orchestration lives in `main.cpp` + `RunMatrixOrchestrator`. `ISimulation::run(composition, output_path)` cannot express mode, plugin set, or `num_threads`. | Low | Always | `grep -n ISimulation` — the interface is only declared; no `class … : public ISimulation`. | Lack of time | If graders expect a Simulator class, add a thin `SimulationImpl` that implements `ISimulation` for a single composition and keep the two-mode CLI as the real entry point — or document in the HLD why `ISimulation` is unused. |
| 17 | Feature | developed in a different way | Mandatory wall-collision throw from `MockMovement` is caught only in `SimulationRunImpl`, not in `DroneControlImpl`. The Common-issues table lists both as targets. `DroneControlImpl` comment says "do not catch here". | Low | Always | Algorithm (or a stub) advances into an occupied voxel. `MockMovement::advance`/`elevate` throws `std::runtime_error`; `DroneControlImpl::step` does not catch; `SimulationRunImpl::run` catches, logs `MISSION_EXCEPTION`, score `-1`. | Lack of time | Catch in `DroneControlImpl`, return `DroneStepStatus::Error`, and keep the SimulationRun boundary catch for anything that still escapes. |
| 18 | Feature | developed in a different way | Mapping algorithm treats `Unmapped` voxels as passable (only confirmed `Occupied` / `OutOfBounds` block the sphere). First contact with an unknown wall can still hit MockMovement's mandatory throw; stall recovery then marks the cell blocked. | Medium | Always | Start a mission whose planned path goes through Unmapped cells that are walls on the hidden map. Expect `MISSION_EXCEPTION` / collision rather than a preemptive illegal-move refusal. See `MappingAlgorithmFrontier.cpp` `isSpherePassable`. | Lack of knowledge | Treating Unmapped as blocked stalled exploration (ex2). A conservative "unknown = wall" planner would miss coverage; we rely on stall detection instead. |
| 19 | Code | Other | `UserCommon/` is compiled only into the Simulator (and its tests), not into Algorithm or MissionControl, even though the assignment defines that folder as files required by more than one project. Algorithm/MissionControl "may" write error logs but do not use `RunErrorLog`. | Low | Not relevant | --- | Lack of time | If MC/Algorithm grow their own logs, `git mv` is already done — add `UserCommon` sources + include dir to those `CMakeLists.txt`. Until a second consumer exists this is a placement smell, not a runtime bug. |
