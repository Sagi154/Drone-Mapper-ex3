# Known Issues (working file)

Working list for the optional staff Known Issues excel. Column schema matches
`context/Known Issues.docx` (example table). English only.

**Do not submit this markdown.** Before packaging, copy the table into the staff
Google Sheet clone and export `.xlsx` into the zip root. Agent instructions:
`.cursor/skills/populate-known-issues/SKILL.md`.

Resolved and stale rows are removed (verbose flag, default-composition scoring,
README, HLD, MockMovement catch in DroneControl, UserCommon-only-in-Simulator,
scan-batch hang, foreign-MC step inflation, CI9 continue-on-Error, CI2 world-OOB
ignore, CI10 mission-bounds clamp, CI3 invalid-command retry then throw, CI8
split-oversize fragments). Numbers are compacted `1..n`. Rows 9–16 are deferred
AdvCpp rubric leftovers from
`docs/superpowers/plans/2026-09-06-advcpp-rubric-findings-fix.md`
plus the 2026-09-06 re-verify (explained in `docs/known-issues-explained.md`).

| # | Type | Sub Type | Description | Severity | Reproducibility | Reproduce steps | Reason | Reason notes |
|---|------|----------|-------------|----------|-----------------|-----------------|--------|--------------|
| 1 | Feature | Missing | Optional: empty movement **and** empty scan (NOOP) is not retried N times. | Low | Always | Algorithm returns a Working command with neither movement nor scan. No NOOP retry; mission proceeds or finishes without the PDF recovery. | Lack of time | Optional CI4. Detect both-empty, retry, throw after N. |
| 2 | Feature | Missing | Optional: empty LiDAR vector is not retried N times. | Low | Always | Arrange a scan that returns `{}`. `DroneControlImpl` does not re-scan or throw after N empty results. | Lack of time | Optional CI6. Retry `lidar.scan` up to `kMaxRetries`, then throw. |
| 3 | Feature | Missing | Optional: Movement returning `false` is not retried N times then thrown. | Low | Always | Use a movement driver that returns `false` (not a wall throw). `DroneControlImpl::applyMovement` returns `Continue` on any `success==false` (no message match, no Error). No N-retry then throw. | Lack of time | Optional CI7. Distinct from mandatory CI5 (MockMovement throw). PDF wants retry then throw; we only Continue. Catch in DroneControl and SimulationRun if implemented. |
| 4 | Feature | Missing | Optional: GPS out-of-bound coordinates are not compared to internal pose. | Low | Always | GPS reports OOB while internal pose is in-bounds (or also OOB). No compare/ignore/throw path. | Lack of time | Optional CI11. Ignore if internal in-bounds; throw if both OOB (targets: DroneControl, SimulationRun). |
| 5 | Feature | Missing | Optional: after a move, impossible GPS is not re-read N times then returned as Error. | Low | Always | Movement succeeds, GPS then reports an impossible pose. No GPS retry loop in `DroneControlImpl`. | Lack of time | Optional CI12. Re-read GPS up to `kMaxRetries`, then return Error (not necessarily throw). Target is DroneControl only. |
| 6 | Feature | Missing | Bonus lazy load/unload of `.so` files is not implemented. Plugins are loaded up front on the main thread and `dlclose`d at shutdown. The assignment explicitly allows eager load; lazy load-once is bonus only. | Low | Always | Read `Simulator/src/main.cpp` and `PluginLoader.cpp`: every required `.so` is opened before `RunMatrixOrchestrator::run`; no unload of unused libraries mid-run. | Lack of time | After the mandatory path is solid, load a `.so` on first use, never reload, `dlclose` when no live objects remain. Document in `bonus.txt` with file:line if claimed. |
| 7 | Feature | developed in a different way | Unmapped remains passable (not hard-blocked). Soft Dijkstra cost prefers Empty (1) over Unmapped (`kUnmappedTraversalCost` = 4); recoverable wall throws + fast stall (2 ticks) recover bumps without `MISSION_EXCEPTION`. VAR-01 PASSes via corner-anchored `sphereIntersectsCellBox` (2026-09-06); do not revive the Occupied-AABB execution gate. | Low | Rare | Path through Unmapped that is a hidden wall: expect Continue + replan, not mission Error. | Lack of knowledge | Soft cost + stall, not hard-block Unmapped (ex2 lesson). Treating unknown as wall stalled exploration. Failed Occupied-AABB gate: `docs/archive/2026-09-05-var01-attempt.md`; git `backup/var01-2026-09-05`. Landed fix: `docs/benchmarks/2026-09-06-var01-approach-a.md`. |
| 8 | Bug | Program misbehave | Mapping scores still miss some ex2 bands after VAR-01 Approach A (2026-09-06 serial Release): `house_full` mean **25.86** (cells 47.46 / 21.00 / 20.08 / 14.89); `small_out` large+long **45.38**; `small_room` large+short **67.65**. `large_out` shorts recovered (74.95 / 72.02, wall <3s) — the 2026-09-03 44.56 / 32.00 short-lidar cliff is no longer the measured picture. | Medium | Always | `python3 .cursor/skills/verify-cell-runtime/scripts/time_each_cell.py --build-dir build/opt` (Release, `num_threads=1`). Compare to `.cursor/skills/verify-cell-runtime/SKILL.md` 2026-09-06 baseline. | Lack of time | Queued runner-up plans can still reuse a stale start pose. The 2026-09-06 clearance + local-search-cap landing changed explore order and recovered `large_out` shorts; remaining band gaps are `house_full` and a few outdoor/room cells. |
| 9 | Code | Other | AdvCpp e03: hot-path heading/trig still unwraps through libm in `MappingAlgorithmImpl` pose predict, `PathShaping::stepCostForPath`, `ScanPlanning` travel heading, `MockMovement` advance, `DroneControlImpl::predictedDestination`, `LidarCone`, `BeamMath::pointAlongBeam`, `MapsComparison` quantize, and LOS. | Low | Not relevant | --- | Lack of knowledge | Replacing these with `mp_units` trig changes binary libm rounding and has already drifted the 24-cell score table. MockLidar ring offsets now use `si::sin`/`si::cos`. Keep remaining unwraps unless a dedicated matrix re-run is planned. |
| 10 | Code | Other | AdvCpp e16: several scan/frontier APIs still take `double` centimetre/radian values (`ConeTemplate`, `LidarCone`, `MappingAlgorithmFrontier`, `WavefrontPlanner`) instead of quantity types. | Low | Not relevant | --- | Lack of time | Retype the hot-path signatures and every call site; blast radius is the full 24-cell score table. |
| 11 | Code | Other | AdvCpp e13: raw `const T*` out-params remain on `MatrixCell`, `WavefrontPlanner::plan`, `rankClusters`, and `ScanPlanning::ScoredDirection`. | Low | Not relevant | --- | Lack of time | Replace with references or `std::optional`/`span` across planner and report code. |
| 12 | Code | Other | AdvCpp e22: `ConeTemplateCache::get` returns `const vector&`; `ScanPlanning.h` / `ExplorationPlan.h` still expose `detail` types. | Low | Not relevant | --- | Lack of time | Return by value or a view type; move `detail` types out of published headers. |
| 13 | Code | Other | AdvCpp e21 leftover: string-pull re-walks in `PathShaping`, `VoxelStamp::mark` re-quantizes (`ConeTemplate.cpp:45`), `findPathTo` does not reuse the `runBoundedSearch` memo, plus extra double walks in `buildSweepDirections`, `unmappedInColumnBelow`, `compareMaps` two grid passes, and `ScanResultToVoxels` mark+supplement. | Low | Not relevant | --- | Lack of time | Each site needs its own hot-path correctness argument; bundling them risks an unattributable score regression. |
| 14 | Code | Code duplication | AdvCpp e10 leftover: `LidarCone`/`ConeTemplate` walk overlap, `MockLidar::scan` ring rebuild, `PluginLoader` directory loops, plus similar bind/CLI-filesystem/`isUnsetBoundaries` copies. | Low | Not relevant | --- | Lack of time | Deduplicate the remaining loops after the load-flow share; keep comparative and competition error messages distinct. |
| 15 | Code | Other | AdvCpp e15/e14: comparative sequence shows `Main` calling `expandRunMatrix`/`distributeWork`; live path is `main` → `runPluginMatrix`. Class diagram omits `runMissionSteps`, `ConfigParseResult`, `TimeFormat`, `BeamMath`, `LidarCone`. | Low | Not relevant | --- | Lack of time | Add a `runPluginMatrix` participant in `docs/hld/seq-comparative-cell.mmd`, add the missing modules to the class diagram, regenerate `HLD.pdf`. |
| 16 | Code | Use of hard coded numbers instead of enums / constants | AdvCpp e23 leftovers after Task 14: stamp pad `+ 2.0` (`ConeTemplate.cpp:23`), LOS `step * 0.5`, PathShaping `1e-9`, LidarCone fallback `1.0` cm, MockLidar `360.0 * deg`. | Low | Not relevant | --- | Lack of time | Name each remaining literal. One row covers all leftover magic numbers. |
