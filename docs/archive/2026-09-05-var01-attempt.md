# Parked: VAR-01 foreign-host illegal-move attempt (2026-09-05)

`known-issues-fixes` no longer contains this work. Restore it only from the backup below; do not treat this as the live algorithm.

## Why it was reverted

An Occupied-AABB execution gate (`5c8c6a2`, Option A) made `skeleton_host` report `HOST_ILLEGAL_MOVE_ATTEMPTS=0` (independence **VAR-01**). Follow-ups (shrink-until-clear, scan-gated descend patience, floor-support `cz >= vz1`, attic-first) unstuck `house_full` from ~7 @ 500 steps, but a 24-cell Release run then **FAIL**ed:

- `large_out` · large · short wall **65.3 s** (≥ 60 s hard-stop)
- `large_out` small-drone cells quit at 600–700 steps (scores ~15–18 vs 2026-09-03 **82 / 45**)
- other outdoor / `small_room` small·short score gaps vs the 2026-09-03 baseline

## Backup (local only — not on `origin`)

| What | Where |
|------|--------|
| Branch (full tree + WIP) | `backup/var01-2026-09-05` @ `524056a` |
| Tag | `backup-var01-2026-09-05` |
| Option A checkpoint | `5c8c6a2` (`fix: checkpoint Option A VAR-01 downward-elevate gate`) |
| 24-cell CSV on that branch | `docs/benchmarks/2026-09-05-var01-floor-24cell-wall.csv` |

Restore the parked tree:

```bash
git checkout backup/var01-2026-09-05
```

Or inspect without switching:

```bash
git show backup-var01-2026-09-05
```

This branch was **not pushed**. If you need it on the remote: `git push -u origin backup/var01-2026-09-05` and `git push origin backup-var01-2026-09-05`.

## Live branch after revert

`known-issues-fixes` was reset to `77951e7` (parent of the Option A commit). Algorithm execution is again the pre-gate policy: Unmapped walkable in Dijkstra, no Option A / shrink / floor-support / attic-first.
