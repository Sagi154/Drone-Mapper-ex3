# Low max_steps for VAR-03 adversarial containment checks (not a staff mission).
mission_config:
  max_steps: 25
  boundaries:
    x_boundary:
      min_cm:  0
      max_cm:  200
    y_boundary:
      min_cm: 90
      max_cm: 200
    height_boundary:
      min_cm: 0
      max_cm: 90
  gps_resolution_cm: 5
